# lettuce-contracts: status notes

Facts about `lettuce-contracts` that are not architecture. The crate README describes the current design.

## Not wired yet

- The crate description names versioned requests, responses, events and generated frontend bindings. Only the provider catalog, account-scoped model discovery and key verification contracts exist; nothing is generated for the frontend yet, and the contracts carry no version field.
